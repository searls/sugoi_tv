#!/usr/bin/env bash
# prove_it: fast tests — runs every time Claude finishes a response
# Runs swift test against SugoiCore package for sub-second feedback
set -e
trap 'prove_it record --name fast-tests --result $?' EXIT

cd "$(dirname "$0")/../SugoiCore"
swift test 2>&1
